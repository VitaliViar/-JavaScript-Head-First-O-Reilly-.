# Посмотрите, как легко пишется код JavaScript
```javascript
var price = 28.99; /*Создать переменную price и присвоить ей значение 28.99.*/
var discount = 10; /*Создать переменную discount и присвоить ей значение 10.*/
var total = 
price - (price * (discount / 100)); /*Вычислить цену со скидкой и присвоить переменной total.*/
if (total > 25) { /*Сравнить переменную total и 25. Если переменная больше...*/
freeShipping(); /*...выполнить фрагмент кода freeShipping.*/
} /*Конец команды if */

var count = 10; /*Создать переменную count и присвоить ей значение 10.*/
while (count > 0) { /*Пока значение count остается больше 0...*/
juggle(); /*...что-то сделать, а потом...*/
count = count - 1; /*...уменьшить count на 1.*/
} /*Конец цикла while*/

var dog = {name: "Rover", weight: 35}; /*Создать переменную dog с атрибутами name и weight.*/
if (dog.weight > 30) {  /*Если атрибут weight больше 30...*/
alert("WOOF WOOF"); /*...вывести на веб-странице сообщение “WOOF WOOF»*/
} else { /*В противном случае...*/
alert("woof woof"); /*...вывести на веб-странице сообщение «woof woof»*/
} /*Конец команды if/else*/

var circleRadius = 20; /*Создать переменную circleRadius и присвоить ей значение 20.*/
var circleArea = /*Создать переменную с именем circleArea...*/
Math.PI * (circleRadius * circleRadius); /*...и присвоить ей результат выражения
(1256.6370614359173)*/
```
